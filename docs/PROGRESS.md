# Прогресс

## Сейчас

- **Дата:** 2026-09-17 (день 1)
- **Сделано сегодня:** git init; uv-проект `mechanic` (Python 3.12); симулятор Torque + приёмник + SQLite + API управления (24 теста зелёные, живой smoke-тест пройден); дамп Mechanics SE → `data/processed/stackexchange.jsonl` (20 996 тредов); парсеры carcarekiosk и startmycar написаны и запущены в фоне.
- **В процессе:** фоновой скрапинг (лог: `data/cache/scrape_carcarekiosk.log`, затем `data/cache/scrape_startmycar.log`; кэш HTML: `data/cache/http/`, повторный запуск берёт из кэша). Результаты → `data/processed/{carcarekiosk,startmycar}.jsonl`.
- **Следующий шаг:** проверить результаты скрапинга (кол-во документов по машинам, качество текста) → записать цифры в NOTES.md → закоммитить день 1 (с разрешения пользователя) → день 2: база DTC, поисковый индекс, тулы.
- **Ждём от пользователя:** аккаунт Vast.ai + пополнение ~$10–15 (к дню 3).

## Браузер (где остановились)

Состояние не должно зависеть от того, жив ли Browser pane. Чтобы восстановиться:
- Dev-серверы запускаются по имени из `.claude/launch.json` → `preview_start backend` (FastAPI на :8000, `--reload`).
- В будущем приложение будет хранить сессию в URL (`?session=…`) + SQLite на сервере.

| Когда | URL | Что делали / где остановились |
|---|---|---|
| 2026-09-17 | http://localhost:8000/docs | Swagger backend-а: `POST /api/sim/{device}` запускает симулятор, `GET /api/sensors/{device}` — показания + DTC |

## Vast.ai

| Когда | Инстанс | GPU | $/ч | Статус |
|---|---|---|---|---|
| — | — | — | — | — |

> Перед завершением работы — проверить, что инстанс остановлен.

## План по дням

- [ ] **День 1 (Mac)** — ~~каркас репо~~ ✅; ~~симулятор Torque~~ ✅; ~~выбор 4 машин + форума~~ ✅; ~~парсер SE~~ ✅; парсеры carcarekiosk / startmycar ⏳ (идёт скрапинг)
- [ ] **День 2 (Mac)** — база DTC; индекс поиска; все тулы; логика агента на локальной модели; 30 тестовых сценариев
- [ ] **День 3 (GPU)** — бенчмарк LLM (4 варианта) и TTS (5 кандидатов); выбор связки ≤16 ГБ
- [ ] **День 4 (GPU)** — Pipecat pipeline по WebSocket: VAD → ASR (CPU) → LLM → TTS; стриминг; barge-in
- [ ] **День 5 (Mac+GPU)** — фронт: разговор, tool-карточки, Garage, Latency-панель, benchmark-режим
- [ ] **День 6 (GPU)** — оптимизация задержки, промпт, прогон сценариев
- [ ] **День 7 (Mac)** — деплой (Cloudflare Pages + Tunnel), `deploy.sh`, README, демо-видео

## Лог

- **2026-09-17** — выбрана тема, стек, план (см. DECISIONS.md). Созданы файлы памяти проекта.
- **2026-09-17** — день 1: выбраны машины и форум по замерам данных; симулятор Torque работает end-to-end; SE-дамп обработан; запущен скрапинг сайтов.
