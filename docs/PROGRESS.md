# Прогресс

## Сейчас

- **Дата:** 2026-09-18 (день 3, старт)
- **Готово:** дни 1–2 полностью. Данные: SE 20 996 тредов, carcarekiosk 238 док., startmycar 5430 англ. отчётов, OBDex 9533 кода. Гибридный поиск (bm25s + bge-small, RRF, буст по машине), 8 тулов, агентский цикл со стримингом, 32 сценария + eval-раннер, 57 тестов зелёные. Коммиты до `c08d1b5`.
- **Индекс:** BM25 готов (56 207 пассажей, запрос ~7 мс). `data/index/dense.npy` НЕТ — считаем на арендованной машине (`build --dense-only`), локально ноутбук перегревался.
- **Vast.ai:** аккаунт есть, `VAST_API_KEY` вписан в `.env` (2026-09-18). `VAST_INSTANCE_ID` пустой — заполнить после аренды. Инстанс ещё НЕ арендован.
- **Следующий шаг:** день 3 по [GPU.md](GPU.md) — арендовать 16 ГБ GPU, поднять автостоп, deploy key, залить код+данные, посчитать dense-индекс, прогнать 4 LLM и 5 TTS, записать цифры в NOTES.md → «Бенчмарки».
- **Репозиторий:** `git@github.com:DanilAra7/voice-mechanic.git` (приватный). Коммиты подписаны `175244557+DanilAra7@users.noreply.github.com`. На инстанс — только через read-only deploy key (генерим на инстансе, публичную половину добавляет пользователь).
- **Конфиг:** `.env` читается `src/mechanic/config.py` → `load_env()` (без зависимостей, реальные env-переменные приоритетнее файла); вызывается в `server.py` и `scripts/idle_shutdown.py`.
- **Контекст:** в `.claude/settings.local.json` стоял `"autoCompactWindow": 130000` — он реально резал окно сессии до 130k (панель контекста показывала ровно эту цифру). Ключ удалён 2026-09-18, осталось только `autoCompactEnabled: true`. Применяется при старте сессии, поэтому после правки нужен рестарт. Настройку окна руками больше не трогаем; непрерывность держим на docs/*.md, `/compact` пользователь зовёт сам.
- **Деньги:** инстанс тарифицируется по часам. Автостоп — `scripts/idle_shutdown.py --idle-minutes 20`. В конце каждой сессии проверять, что инстанс остановлен, и писать это в таблицу Vast.ai ниже.

## Браузер (где остановились)

Состояние не должно зависеть от того, жив ли Browser pane. Чтобы восстановиться:
- Dev-серверы запускаются по имени из `.claude/launch.json` → `preview_start backend` (FastAPI на :8000, `--reload`).
- В будущем приложение будет хранить сессию в URL (`?session=…`) + SQLite на сервере.

| Когда | URL | Что делали / где остановились |
|---|---|---|
| 2026-09-17 | http://localhost:8000/docs | Swagger backend-а: `POST /api/sim/{device}` запускает симулятор, `GET /api/sensors/{device}` — показания + DTC |

## Vast.ai

| Когда | Инстанс | GPU | $/ч | Статус |
|---|---|---|---|---|
| — | — | — | — | — |

> Перед завершением работы — проверить, что инстанс остановлен.

## План по дням

- [x] **День 1 (Mac)** — каркас репо; симулятор Torque; выбор машин и форума; все три парсера данных
- [ ] **День 2 (Mac)** — ~~база DTC~~ ✅; ~~тулы~~ ✅; ~~агентский цикл~~ ✅; ~~32 сценария + раннер~~ ✅; индекс ⏳
- [ ] **День 3 (GPU)** — бенчмарк LLM (4 варианта) и TTS (5 кандидатов); выбор связки ≤16 ГБ
- [ ] **День 4 (GPU)** — Pipecat pipeline по WebSocket: VAD → ASR (CPU) → LLM → TTS; стриминг; barge-in
- [ ] **День 5 (Mac+GPU)** — фронт: разговор, tool-карточки, Garage, Latency-панель, benchmark-режим
- [ ] **День 6 (GPU)** — оптимизация задержки, промпт, прогон сценариев
- [ ] **День 7 (Mac)** — деплой (Cloudflare Pages + Tunnel), `deploy.sh`, README, демо-видео

## Лог

- **2026-09-17** — выбрана тема, стек, план (см. DECISIONS.md). Созданы файлы памяти проекта.
- **2026-09-17** — день 1: выбраны машины и форум по замерам данных; симулятор Torque работает end-to-end; SE-дамп обработан; запущен скрапинг сайтов. Коммит `6f4370f`.
- **2026-09-17** — день 2: DTC-база, поиск, тулы, агентский цикл, eval-харнесс.
- **2026-09-18** — конфиг через `.env` (`load_env()`), ключ Vast вписан пользователем. Начат день 3.
