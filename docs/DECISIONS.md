# Журнал решений

Формат: дата · решение · почему · что отвергли.

## Принятые

| Дата | Решение | Почему | Отвергли |
|---|---|---|---|
| 2026-09-17 | Тема — голосовой автомеханик | Голос оправдан (руки заняты в гараже), много естественных тулов, у пользователя Audi A4 | кухонный ассистент, D&D-мастер, гид по небу, travel-консьерж |
| 2026-09-17 | Лимит VRAM всего стека — 16 ГБ | Требование пользователя (строже исходных 18 ГБ) | — |
| 2026-09-17 | Только браузерный клиент | Требование; сдаём сайт | CLI, телефония |
| 2026-09-17 | Kokoro отвергнут, TTS выбираем A/B-тестом среди выразительных моделей | Пользователь хочет качество уровня ElevenLabs | Kokoro-82M (быстрый, но голос хуже) |
| 2026-09-17 | LLM выбираем бенчмарком из 4 вариантов (gpt-oss-20b, Qwen3-30B-A3B в двух конфигурациях, Qwen3-14B AWQ) | Нужно обоснование цифрами: ум vs задержка vs VRAM | Модели >30B (только с тяжёлым CPU-offload → задержка) |
| 2026-09-17 | Данные: carcarekiosk + startmycar + **один** форум, парсим заранее, поиск локальный | Без внешнего поискового API: быстрее, надёжнее на демо, бесплатно | Brave/SearXNG live-поиск |
| 2026-09-17 | Датчики — только симулятор, но в протоколе Torque web-upload | Пользователь не хочет ходить в гараж; лид сможет сам «ломать» машину; реальный Torque подключится без правок | реальные данные с авто в демо |
| 2026-09-17 | ASR на CPU | Освободить VRAM под качественный TTS | ASR на GPU |
| 2026-09-17 | Хостинг: Vast.ai почасово (карта 16 ГБ) + статический фронт бесплатно + Cloudflare Tunnel | Минимум денег; лид рекомендовал Vast.ai; 16 ГБ карта = честный лимит | постоянный VPS |
| 2026-09-17 | Транспорт аудио — WebSocket | Cloudflare Tunnel не пропускает UDP (WebRTC) | WebRTC |
| 2026-09-17 | Разработка не-GPU частей локально на Mac | Экономия GPU-часов | всё на арендованном GPU |
| 2026-09-17 | Audi A4 входит в 5 машин MVP, остальные 4 — по объёму данных | Машина пользователя | — |
| 2026-09-17 | Audi A4 — поколение **B8 (2009–2016)**, 2.0 TFSI | Машина пользователя | B9 |
| 2026-09-17 | Форум = **mechanics.stackexchange.com**, из официального дампа archive.org `stackexchange_20251231` | CC BY-SA, парсинг не нужен (robots.txt сайта запрещает ботов целиком, а дамп — легальный путь), 28k вопросов / 40k ответов, поиск по всем тредам (общие знания применимы к любой машине) с бустом по марке/модели | audizine/audiworld (только Audi), 2carpros |
| 2026-09-17 | 4 машины по объёму данных (SE-теги + отчёты startmycar + страницы carcarekiosk): **Honda Accord 9th 2013–17 (2.4)**, **Ford F-150 13th 2015–20 (2.7 EcoBoost)**, **Honda Civic 10th 2016–21 (2.0)**, **Toyota Corolla 11th 2014–19 (1.8)** | Замер 2026-09-17: SE-теги civic 586 / accord 413 / camry 270 / corolla 269 / f-150 136 / a4 37; startmycar accord 2282 / f-150 2025 / corolla 1000 / civic 708 / camry 540 / a4 338 | Camry (меньше отчётов, чем Corolla), Jetta/Golf |
| 2026-09-17 | Протокол датчиков = Torque Pro web upload: `GET ?v&session&id&eml&time&k<hexpid>=val&userFullName<pid>&userShortName<pid>&userUnit<pid>&defaultUnit<pid>`, ответ `OK!` | Сверено с исходниками Home Assistant `components/torque` и econpy/torque `upload_data.php` | — |
| 2026-09-17 | DTC в симуляторе пишутся напрямую в store (callback), не через HTTP | Torque web upload, по имеющимся данным, коды ошибок не передаёт (?) | Выдуманное расширение протокола |
| 2026-09-17 | Симулятор: 6 неисправностей (coolant_leak, thermostat_stuck_open, misfire_cyl1, vacuum_leak, dirty_maf, failing_alternator), 3 режима (idle/city/highway), `time_scale` для ускорения | У каждой — узнаваемая сигнатура в данных, чтобы агенту было над чем рассуждать (напр. vacuum leak: STFT высокий на холостых, падает на круизе) | — |
| 2026-09-17 | Данные сайтов: carcarekiosk — только выбранные поколения; startmycar — все годы модели, только англ. отчёты, детальные страницы только при наличии ответов | Экономия запросов; `vehicle_ids` проставлен только при попадании года в поколение | — |

| 2026-09-18 | **LLM = gpt-oss-20b MXFP4, `reasoning_effort: low`, контекст 8k** | Единственная модель, рядом с которой помещается стримящий TTS. Сквозной замер: 15 875 из 16 376 MiB, 655 мс до первого звука | Qwen3-30B-A3B Q2 (лучше по сценариям 71.9% против 65.6%, но с ней не влезает ни один голос), Q3 (14.9 ГБ), Q4 (не влезает без CPU-offload), Qwen3-14B (плотная → 35 tok/s) |
| 2026-09-18 | **TTS = Kyutai TTS 1.6B** | Стримит: 350 мс до первого звука против 1186 мс у нестримящего Chatterbox при той же RTF ~0.42 | Chatterbox (качество есть, стриминга нет), Qwen3-TTS (нет открытых весов), VibeVoice-Realtime (нет streaming-кода в пакете), Orpheus 3B (не влезает) |
| 2026-09-18 | `reasoning_effort: low` задаём **на сервере**, а не в клиенте | При умолчании gpt-oss думает 3.2 с перед первым словом, при `high` вообще не доходит до ответа в разумном бюджете токенов | оставить на умолчании |
| 2026-09-18 | Текущая машина — **в системном промпте**, а не только в туле `get_vehicle` | Замер: без этого агент тратит раунд тула или спрашивает водителя о том, что сессия уже знает. Точность вызова тулов +6…12 п.п. у всех четырёх моделей | держать всё ситуативное в тулах ради кэшируемости префикса |

| 2026-09-18 | **ASR = parakeet-tdt-0.6b-v2 int8, ОФЛАЙНОВЫЙ, sherpa-onnx на CPU** | Хвост 104 мс при RTF 0.027. Потоковая сборка той же модели даёт RTF 1.69 — не успевает за речью и копит отставание | потоковая parakeet-unified-0.6b (не успевает), parakeet 110m (34 мс, но приписывает слова — точность важнее 70 мс) |

| 2026-09-18 | **Транспорт пишем сами, Pipecat не берём** (отменяет решение от 2026-09-17) | Агентский цикл к этому моменту оброс своим: тулы, фразы-заглушки, guardrail до модели, стриминг по предложениям и тайминги. Тайминги — часть того, что сдаём, и нужен точный контроль над метками. Pipecat пришлось бы гнуть под всё это, превращая наш цикл в его процессор | Pipecat (даёт перебивание и планирование кадров из коробки, но ценой переписывания цикла) |
| 2026-09-18 | Оркестровка разговора (`voice/session.py`) **не зависит от транспорта** | Тестируется без браузера, WebSocket и GPU: 7 тестов на подменённых моделях проверяют порядок этапов, отсчёт от конца речи и перебивание | логика прямо в WebSocket-обработчике |

| 2026-09-20 | **Гараж показывает тестирующему скрытую причину неисправности, агенту — никогда** | В этом весь смысл демо: лид сам ломает машину, знает ответ и проверяет, дойдёт ли до него агент по одним датчикам. `Fault.hidden_cause` уходит в браузер через `/api/catalog`, но не в промпт и не в тулы | показывать неисправность в диалоге (нечего было бы проверять); не показывать вовсе (лид не знает, прав ли агент) |
| 2026-09-20 | Смена машины в Гараже **сбрасывает диалог** и перестраивает системный префикс | Агент называет текущую машину в системном промпте; без сброса вся предыдущая история про другую машину читается как про эту | менять машину молча |
| 2026-09-20 | Смена неисправности и режима на **работающей** машине — через `PATCH`, а не перезапуск | Двигатель остаётся прогретым, история показаний сохраняется — это и есть «сломалось на ходу». Перезапуск нужен только при смене машины | всегда перезапускать симулятор |
| 2026-09-20 | **Длина ответа ограничена кодом** (140 символов озвученного текста, режем между предложениями) | Без лимита агент отвечал 36 секундами речи. Промпт просит краткости, модель соглашается и всё равно говорит шесть предложений. Медиана ответа 24 → 12.4 с, при этом содержательная точность выросла 87.9 → 93.9 % | просить краткости только промптом; обрезать по токенам (рвёт предложение) |
| 2026-09-20 | **Дословные фразы синтезируются при старте** и играются из кэша | Заглушки и предупреждения — первое, что слышит водитель. Первый звук p50 1496 → 1031 мс, синтез ушёл с критического пути (`first_audio` = `first_sentence` + 1 мс). Слова и голос те же, качество не меняется | синтезировать каждый раз заново |
| 2026-09-20 | Потоки ASR = `min(8, бюджет cgroup)`, бюджет читаем из `/sys/fs/cgroup/cpu.max` | `os.cpu_count()` отдаёт ядра хоста (64 при нашей доле 15.36). Медиана ASR 345 → 265 мс | фиксированные 4 потока |
| 2026-09-20 | `reset` (кнопка «New conversation») **отменяет текущий турн** | Ответ продолжал звучать поверх следующего вопроса и попадал в его замер | сбрасывать только историю диалога |
| 2026-09-20 | **На паузу инстанс ОСТАНАВЛИВАЕМ, а не уничтожаем** (отменяет решение дня 4) | Поднять заново = 21.6 ГБ трафика = $0.85 на машине с $39/ТБ. Остановленный платит $0.019/ч за диск: окупается 45 часов простоя, до дедлайна уничтожать невыгодно | уничтожать (правило писалось из расчёта $2.67/ТБ) |
| 2026-09-20 | Оффер выбираем **с фильтром `inet_down_cost`** | Цена часа у офферов отличается на центы, цена трафика — в 15 раз. 95 % счёта сессии — скачивание | смотреть только `dph_total` |
| 2026-09-20 | **Эмоциональность голоса меняем образцом, а не температурой** (выбор образца — за пользователем, слушает) | Замер: `temp 0.4` вместо 0.6 добавляет 15–40 % времени речи при том же тексте — низкая температура даёт длинные паузы, а не ровный тон. Спокойный образец того же диктора стоит +4…8 % длины ответа; первый звук (616–622 мс) от образца не зависит вовсе | крутить температуру; сменить диктора (+17 % времени и другой человек) |

## Открытые вопросы

- **Язык README и кода** (код и комментарии уже на английском; README — предположительно английский).
- ~~Лицензия датасета OBD-II кодов~~ → решено ниже.

## Кандидаты, выбранные заранее (реализация — день 2)

- **База DTC = [OBDex](https://github.com/foerbsnavi/OBDex)**: данные CC0, код MIT. `data/generic/{P0,P2,P3,B0,C0,U0,U3}xxx_enriched.yaml` (~9.5k кодов): title/description (en/de), `affected_components`, `common_causes` с `likelihood`, `symptoms`. Есть `data/pids/mode01.yaml`. Альтернатива mytrile/obd-trouble-codes (MIT, только названия) — беднее.
- ToS carcarekiosk / startmycar явно про скрапинг ничего не нашли (grep по /terms пустой — возможно, страница рендерится JS) (?). robots.txt разрешает нужные пути. Парсим 1 запрос/с, с кэшем.
